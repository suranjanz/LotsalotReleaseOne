Based On UK Lotto
Wasm Blazor WebAssembly Project No ThirdParty Libraries. No Jscript. Total C# HTML5 CSS only
data is real and live and can be refreshed by the user. no database. just localstore CSV and Json text. so if its lost by cache clean...so what. 
Localstore CRUD
can copy paste CSV of your tickets to clipboard and save them outside the app if desired. Import later.
DrawResults archive since 2013. can set a date range or simply add/remove any year. or via crud. or via API call for the latest.
Every component is plug and play. every container you see. so each component handles its own UI and its own dedicated services to handle the business logic.
its not very stylish. but to prove the point it can be done. front enders can approach that angle. this is a web page based CSS so for mobiles view in desktop mode.

so there are somepending changes of to do's but getting it live is a giant leap.

his is fully working though. click everywhere or hover the mouse to get all the features and understand the structure. notice you can Bank numbers from almost everywhere
in the UI and easily add that to your new tickets. currently use the ADD button. i dont like the way save overwrites what ever is the current row unless you remember to change the ID field
look for the source code in the next repository
